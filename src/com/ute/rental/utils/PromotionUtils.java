package com.ute.rental.utils;

import java.util.ArrayList;


import com.ute.rental.bo.PromotionDetails;
import com.ute.rental.viewbo.PromotionView;

public class PromotionUtils {
	/*public  static ArrayList<PromotionView> covertToCarViewList(ArrayList<PromotionDetails> details){
		ArrayList<PromotionView> promotionViewList = new ArrayList<PromotionView>();
		for(PromotionView pDetails  : promotionViewList) {
			promotionViewList.add(converToPromotionView(pDetails));
		}
		return promotionViewList;
	}

	private static PromotionView converToPromotionView(PromotionView pDetails) {
			PromotionView promotionView = new PromotionView();
			PromotionDetails prDetails = new PromotionDetails();
			promotionView.setId_car(prDetails.getCar().getId_car());
			promotionView.setSpeciesID(prDetails.getCar().getSpeciesID());
			promotionView.setManufacID(prDetails.getCar().getManufacID());
			promotionView.setNameCar(prDetails.getCar().getNameCar());
			promotionView.setAvatar(prDetails.getCar().getAvatar());
			promotionView.setColor(prDetails.getCar().getColor());			
			promotionView.setQuantity(prDetails.getQuantity());
			promotionView.setPrice(prDetails.getCar().getPrice());
			promotionView.setPricehour(prDetails.getCar().getPricehour());
			return promotionView;
	}*/
}
